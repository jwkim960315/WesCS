import React from 'react';

import LoadingContainer from './LoadingContainer.styles';

const Loading = () => (
    <LoadingContainer>
        <div>LOGO</div>
        <div style={{ marginLeft: '10px' }}>Please Wait...</div>
    </LoadingContainer>
);

export default Loading;